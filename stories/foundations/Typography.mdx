import { Meta } from '@storybook/addon-docs/blocks'
import { HEADINGS, LINE_HEIGHTS, TEXT_COLORS, FONT_WEIGHTS, MIN_SIZES } from '../../tokens/typography'
import { SpecTable, Code, DoDont } from './_parts'

<Meta title="基礎/文字" />

# Typography

- フォントの SSOT: `tokens/theme.css` の `--font-sans`
- 運用ルール: `ai-dev-standards/typography.md`

```
Inter, IBM Plex Sans JP, ui-sans-serif, system-ui, sans-serif, （絵文字フォント）
```

欧文は Inter、和文は IBM Plex Sans JP を使います。
どちらも読み込めない環境では OS 標準のサンセリフにフォールバックします。
`font-sans` 以外のフォントファミリを画面ごとに指定しないでください。

### 縦位置の光学補正

`tokens/fonts.css` で Inter に `ascent-override: 98%` / `descent-override: 23%` を
指定しています。**外すとボタンや入力欄の中で文字が上にずれます。**

固定高さ + `align-items: center` のコントロールでは、ベースラインの位置は
`line-height` ではなく `(ascent − descent) ÷ 2` だけで決まります。
行ボックスの基準（strut）はスタック先頭の Inter が持ちますが、和文グリフは
IBM Plex Sans JP から来るため、両者の視覚的な中心がずれます。

<SpecTable
  head={['基準', '計算', 'ベースラインからの距離']}
  rows={[
    ['Inter の箱の中心', '(1984 − 494) ÷ 2 ÷ 2048', '0.36377em'],
    ['IBM Plex Sans JP の仮想ボディ中心', '(880 − 120) ÷ 2 ÷ 1000', '0.38000em'],
    ['差', '—', '0.01623em（14px で 0.23px）'],
  ]}
/>

この差に加えてブラウザがベースラインをピクセル境界に丸めるため、補正前は
和文が平均 0.505px 上にずれていました。補正後は 0.130px です。

`ascent + descent = 121%` は元の Inter（120.996%）を維持しているため、
**行ボックスの高さは変わりません**。ベースラインの位置だけが下がります。

フォントを差し替えるときは再計算が必要です。導出手順は `tokens/fonts.css`
のコメントにあります。

---

## 見出しスケール

<SpecTable
  head={['レベル', '見本', 'クラス', 'サイズ', '用途']}
  rows={HEADINGS.map((h) => [
    h.level,
    <span className={h.cls}>見出しサンプル</span>,
    <Code>{h.cls}</Code>,
    h.px,
    h.usage,
  ])}
/>

**見出しレベルは飛ばさない**でください（H1 の次に H3 を置かない）。
スクリーンリーダーは見出しの階層で文書構造を読み取るため、
飛ばすと目次が壊れます。

**サイズだけで見出しを作らない**でください。
見た目を小さくしたい場合も、要素は `<h2>` のままクラスで調整します。

<DoDont
  doText={<><Code>{'<h2 className="text-xl font-semibold">'}</Code>（要素は正しい階層、見た目はクラスで調整）</>}
  dontText={<><Code>{'<div className="text-3xl font-bold">'}</Code>（見出しに見えるが構造上は本文）</>}
/>

---

## 行間

<SpecTable
  head={['クラス', '値', '用途']}
  rows={LINE_HEIGHTS.map((l) => [<Code>{l.cls}</Code>, l.value, l.usage])}
/>

長い本文には `leading-relaxed`、見出しには `leading-tight` を使います。
見出しは行間が広いと 1 つの見出しに見えなくなります。

---

## フォントウェイト

<SpecTable
  head={['クラス', '値', '見本', '用途']}
  rows={FONT_WEIGHTS.map((w) => [
    <Code>{w.cls}</Code>,
    w.value,
    <span className={w.cls}>サンプル Sample 123</span>,
    w.usage,
  ])}
/>

`font-light`（300）以下は使いません。細い和文は視認性が落ちます。

---

## テキストカラーとコントラスト比

コントラスト比は白背景に対する値です。WCAG AA は通常サイズで **4.5:1** 以上が必要です。

<SpecTable
  head={['クラス', '見本', '名称', 'コントラスト比', 'WCAG AA']}
  rows={TEXT_COLORS.map((c) => [
    <Code>{c.cls}</Code>,
    <span className={c.cls}>テキストサンプル</span>,
    c.name,
    c.contrast,
    c.wcag,
  ])}
/>

### 注意事項

- **`text-gray-500` 以下は小さいテキストに使わない**でください。
  補足情報でも読めなければ意味がありません
- **`text-gray-400`（無効テキスト）は WCAG AA を満たしません。**
  そのため無効状態は色だけで示さず、`disabled` 属性・アイコン・文言を併用します
- 新規コンポーネントでは `text-foreground` / `text-muted-foreground` を使ってください。
  上の `text-gray-*` はダークモードに追従しません

---

## 最小サイズ

<SpecTable
  head={['対象', '最小サイズ']}
  rows={[
    ['本文', MIN_SIZES.body],
    ['キャプション・メタ情報', MIN_SIZES.caption],
    ['ボタンのラベル', MIN_SIZES.button],
  ]}
/>

`text-[10px]` 以下は原則使いません。
例外はアバターの頭文字（`avatar-sm`）のように、読む対象ではない文字だけです。

---

## 数値の表示

金額・件数などの数値は**右寄せ**にすると桁が揃って比較しやすくなります
（`Table` の `align: "right"`）。

```tsx
{ key: 'amount', header: '金額', align: 'right',
  cell: (r) => `${r.amount.toLocaleString()} 円` }
```

`toLocaleString()` で桁区切りを入れてください。
`1234567` と `1,234,567` では読み取り速度が大きく変わります。
